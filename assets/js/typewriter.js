/* Typewriter */
const instance = new Typewriter('#typewriter', {
    strings: ['nano lunar rover', 'unmanned American lunar rover', 'student built lunar rover'],
  	autoStart: true,
  	loop: true,
});